<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DatabindingDemo"
        tools:targetApi="31">

        <!--
        <activity
            android:name=".JavaEx2MainActivity"
            android:exported="false" />
        <activity
            android:name=".JavaEx1MainActivity"
            android:exported="false" />
        -->
        <!-- android:name=".example1.MainActivity"  for activity -->
        <!-- FragmentActivityDemo for fragment -->
        <!-- Example3RecylerViewActivity -->
        <activity
            android:name=".javaex6livedata.Ex6LiveDataMainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>